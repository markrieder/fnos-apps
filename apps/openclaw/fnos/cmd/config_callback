#!/bin/bash

### 配置变更后：重启服务使配置生效

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "${SCRIPT_DIR}/common"

log_step "config_callback"
install_log "配置已变更，重启 OpenClaw Gateway..."
stop_openclaw_gateway
exit 0
