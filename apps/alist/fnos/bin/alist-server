#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1

DATA_DIR="$APP_DATA_DIR/data"
mkdir -p "$DATA_DIR"

cd "$APP_DIR" || exit 1
exec ./alist server --data "$APP_DATA_DIR/data" --port ${TRIM_SERVICE_PORT:-5246}
