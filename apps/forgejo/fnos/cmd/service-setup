#!/bin/bash

LOG_FILE="${TRIM_PKGVAR}/${TRIM_APPNAME}.log"
PID_FILE="${TRIM_PKGVAR}/${TRIM_APPNAME}.pid"

APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR="${TRIM_PKGVAR}"

SERVICE_COMMAND="$APP_DIR/bin/forgejo-server $APP_DATA_DIR"
SVC_BACKGROUND=y
SVC_WRITE_PID=y

service_preuninst() {
    # 使用 -x 精确匹配进程名，避免误杀 appcenter-cli 等包含 "forgejo" 参数的进程
    pkill -x "forgejo" 2>/dev/null || true
    sleep 1
    pkill -9 -x "forgejo" 2>/dev/null || true
}

service_poststop() {
    service_preuninst
}
