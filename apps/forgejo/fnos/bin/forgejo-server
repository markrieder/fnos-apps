#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1

DATA_DIR="$APP_DATA_DIR/data"
mkdir -p "$DATA_DIR/custom/conf"

cd "$APP_DIR" || exit 1
exec ./forgejo web --port ${TRIM_SERVICE_PORT} --custom-path "$DATA_DIR/custom" --config "$DATA_DIR/custom/conf/app.ini" --work-path "$DATA_DIR"
