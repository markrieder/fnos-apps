#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1

mkdir -p "$APP_DATA_DIR/data"

cd "$APP_DIR" || exit 1
exec ./Jackett/jackett --NoUpdates --DataFolder "$APP_DATA_DIR/data" --Port ${TRIM_SERVICE_PORT:-9117} --ListenPublic
