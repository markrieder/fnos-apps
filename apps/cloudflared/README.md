# Cloudflare Tunnel for fnOS

Cloudflare Tunnel Web UI package for fnOS, based on `wisdomsky/cloudflared-web` with host networking.

## Local Build

```bash
cd apps/cloudflared && ./update_cloudflared.sh
```
