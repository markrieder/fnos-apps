#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1

mkdir -p "$APP_DATA_DIR/data"

cd "$APP_DIR" || exit 1
exec ./prometheus --config.file="$APP_DATA_DIR/prometheus.yml" --storage.tsdb.path="$APP_DATA_DIR/data" --web.listen-address="0.0.0.0:${TRIM_SERVICE_PORT:-9090}"
